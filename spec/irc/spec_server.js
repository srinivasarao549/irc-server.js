var IRC     = require(path.join(process.cwd(), 'lib', 'irc', 'server')),
    Server = IRC.Server;

describe("An IRC server", function() {
  beforeEach(function() {

  });

  it("should ...", function() {
    // ...
  });
})